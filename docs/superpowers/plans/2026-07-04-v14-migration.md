# Foundry V14 Migration — Technical Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax for tracking. A
> live Foundry **V14** instance is available — every "verify" step is a real
> runtime check, not a guess. Do the verification steps *before* committing the
> code change they gate, so we never ship a speculative fix. Land work on
> `V14-migration`; push convention is `gh auth switch --user YeNov` before push,
> switch back to `yehornovakov` after (see memory `push-as-yenov-account`).

**Goal:** `starwarsffg` runs clean on Foundry VTT V14 (stable 14.359) with
`compatibility.verified: 14` declared, no `ReferenceError` from removed globals,
no legacy-Document-API throws, and no new console deprecation warnings that we
introduced.

**Branch:** `V14-migration` (off `main` at `a95a2c3f`). Currently one commit
ahead — `342736e9` "Namespace bare global helper calls under foundry.utils for
V14" — unpushed.

**Starting state (audited 2026-07-04).** The V2-full migration is complete; the
system is already ~95% V13-namespaced. Confirmed via code audit:

| Area | State |
|---|---|
| Bare util globals — object helpers (`mergeObject`, `setProperty`, `duplicate`, …) | ✅ Fixed in `342736e9` |
| Bare util globals — `randomID()` | ❌ **`342736e9` missed this helper.** 5 bare `randomID()` calls remain and throw `ReferenceError` on V14 — see Stage 1.2 |
| ApplicationV2 / DialogV2 / HandlebarsApplicationMixin | ✅ Used throughout; no `extends Application`, no `new Dialog` |
| `FFGFormApplication` | ✅ Custom class on `HandlebarsApplicationMixin(ApplicationV2)` — not a V1 remnant |
| Dice terms | ✅ All extend `foundry.dice.terms.DiceTerm`; `RollFFG extends Roll` |
| `renderTemplate`/`loadTemplates` | ✅ Namespaced under `foundry.applications.handlebars.*` |
| `TextEditor`/`FilePicker`/`ContextMenu`/`DragDrop`/`Tabs` | ✅ Namespaced under `foundry.applications.ux.*` / `.apps.*` |
| MeasuredTemplate / TinyMCE | ✅ None (TinyMCE only appears in harmless CSS-guard selectors) |

So this migration is a short cleanup, not a rewrite. The plan is ordered by
urgency: Stage 1 = things that block or throw on V14; Stage 2 = polish.

---

## Stage 0 — Baseline capture on V14 (do first)

Establishes the "before" picture so we can attribute every warning/error.

- [ ] **0.1** Launch the system on the V14 instance with `compatibility.maximum`
      still `13`. Record how V14 presents it (hard block vs. compatibility-mode
      warning). This tells us whether Stage 1.1 is a true blocker or just a nag.
  - **Contingency (if V14 hard-blocks load):** the smoke path in 0.2 needs the
    system to actually initialize. If `maximum: 13` prevents that, make a
    **local, uncommitted** edit to `system.json` bumping `maximum`/`verified` to
    14 *just to capture the baseline*, then `git checkout system.json` afterward.
    Step 1.1 remains the single committed manifest change, done last. Do **not**
    commit the manifest bump as part of Stage 0.
- [ ] **0.1a** At load, watch specifically for a `ReferenceError` thrown from
      [groupmanager-ffg.js:7](../../../modules/groupmanager-ffg.js) — it is
      module-level (`const CanvasLayerClass = foundry?.canvas?.layers?.CanvasLayer
      || CanvasLayer;`) and imported eagerly by
      [swffg-main.js:28](../../../modules/swffg-main.js), so if
      `foundry.canvas.layers.CanvasLayer` moved/was removed in V14 the bare
      `CanvasLayer` fallback throws **before any smoke path runs** and kills init.
      This gates Stage 1.6.
- [ ] **0.2** Open the browser console. With the system loaded (compatibility
      mode if needed), exercise the smoke path and capture **all** errors +
      deprecation warnings to a scratch file:
  - Open each actor sheet type: character, minion, rival/nemesis (adversary),
    vehicle, homestead, plus a Codex sheet if the `codex-UI` flag is on.
  - Open each item sheet type (weapon, armour, gear, talent, specialization,
    force power, signature ability, item-modifier, item-attachment).
  - Open the item editor (`{{editor}}` inline ProseMirror mount) — **this is the
    `ProseMirror` global check** (Stage 1.4).
  - Run the OggDude importer (data-importer → import-helpers path).
  - Open the Group Manager (Destiny Tracker menu → Group Manager) and Grant XP.
  - Roll a skill and a weapon with sound flags enabled (`roll-builder.js`).
  - Drag a skill and a weapon to the hotbar, then **execute** both macros —
    **this is the generated-macro `.data.data` / `getData()` check** (Stage 1.3).
- [ ] **0.3** Save the captured console log to
      `docs/superpowers/plans/artifacts/2026-07-04-v14-baseline-console.txt`
      (create the dir). This is the checklist Stage 1 must drive to zero.

---

## Stage 1 — Required (blocks or throws on V14)

Land as one PR (or a small series). **Unconditional fixes** (throw regardless of
V14 API specifics): 1.2 (`randomID`) and 1.3 (`.data.data` + un-awaited
`getData`). **Verify-gated** (fix only if Stage-0 confirms the failure mode, else
demote to Stage 2): 1.4 (`ProseMirror`), 1.5 (`Hotbar`), 1.6 (`CanvasLayer` — but
note 1.6 fails at import time, so it's gated on 0.1a, not 0.2).

### 1.1 Declare V14 compatibility — **do this step LAST in Stage 1**

- [ ] Edit [system.json:6-10](../../../system.json) →
      `"minimum": 13, "verified": 14, "maximum": 14`.
      (Keep `minimum: 13` unless Stage 1 introduces a V14-only API; the code
      changes below are all back-compatible with V13, so 13 stays supported.)
- [ ] Reload on V14: system should load with **no** compatibility banner.
- **Rationale:** gating change for V14 support. Sequenced last so the full
  smoke-test in 0.2 runs against the *old* declaration first (surfacing real
  incompatibilities rather than hiding them behind a version bump).

### 1.2 Finish the bare-global fix — `342736e9` missed `randomID()`

`342736e9` namespaced the object helpers but **did not touch `randomID`**. 5
bare `randomID()` calls remain; each throws `ReferenceError: randomID is not
defined` on V14 when its code path runs. (Audit note: the original "zero
remaining" claim came from a ripgrep pattern using a negative lookbehind, which
ripgrep's default engine silently does not support — it returned a false
"no matches". Re-audited with a `[^\w.]`-prefix pattern; results below.)

- [x] Prefix all 5 sites with `foundry.utils.`: **done — commit `94bc3f53`**
  - [import-helpers.js:1699,1712,1731,1743](../../../modules/importer/import-helpers.js)
    (×4 — OggDude character importer, obligations/duties keys)
  - [actor-sheet-ffg.js:1598](../../../modules/actors/actor-sheet-ffg.js)
    (×1 — `.add-duty` click handler)
- [x] Confirm no *other* bare util-global slipped through: re-run the
      `[^\w.](randomID|mergeObject|setProperty|getProperty|hasProperty|duplicate|deepClone|expandObject|flattenObject|invertObject|filterObject|isObjectEmpty|isNewerVersion|getType|diffObject|encodeURL|isEmpty|debounce)\s*\(`
      grep over `modules/**` — the only remaining hits should be inside comments
      (`item-sheet-ffg.js:1244,1255`, `item-ffg.js:734`), which are inert.
- [ ] Runtime-verify: from Stage 0, exercise the OggDude import (character with
      obligations *and* duties) and the actor-sheet "add duty" button — both hit
      `randomID` — and confirm no `ReferenceError`.

### 1.3 Legacy `.data.data` Document access (latent throws)

Pre-existing bugs (broken since V11), not V14-specific, but they're legacy-API
throws surfaced by this audit. Fix as part of the V14 cleanliness pass.

- [x] **done — commit `325cf0e4`** — [embeddeditem-helpers.js:227](../../../modules/helpers/embeddeditem-helpers.js) —
      `ownedItem.data.data[modifierType]` reads `.data` on a real Item Document
      (`undefined` since V11 → throws). Lines 231-232 immediately below already
      use `ownedItem.system[modifierType]` correctly — mirror that:
      `ownedItem.system[modifierType].findIndex(...)`.
      Verify the branch is reachable (`modifierId` non-numeric) via 0.2's
      item-modifier interactions; if 0.2 never hits it, still fix (dead-simple,
      correct-either-way) but note reachability.
- [x] **done — commit `325cf0e4`** — **Generated skill macros — there are TWO branches, fixed both.** The
      hotbar drop handler emits skill-roll macro command strings in two places:
  - [macros.js:36-40](../../../modules/helpers/macros.js) (`entity.type === "skill"`)
    — uses **stale `.data.data`**: `ffgactor.data.data.skills[...]` /
    `ffgactor.data.data.characteristics[...]`. Rewrite to `ffgactor.system.skills`
    / `ffgactor.system.characteristics`.
  - [macros.js:67-80](../../../modules/helpers/macros.js) (`data?.data?.type === "skill"`)
    — already uses `.system.*` (no `.data.data` bug), but shares the next defect.
  - **Both** branches call `const actorSheet = ffgactor.sheet.getData();`
    **without `await`**. `getData()` is *intentionally bridged* on the FFG V2
    sheets ([ffg-document-sheet.js:163,188](../../../modules/apps/ffg-document-sheet.js)
    → `_prepareContext` calls `getData`) and
    [`ActorSheetFFG.getData` is **async**](../../../modules/actors/actor-sheet-ffg.js) —
    so un-awaited it hands `rollSkillDirect` a **Promise**, not sheet data.
    **Do not** swap it for `_prepareContext()` — `getData()` is the correct bridge
    and produces exactly the legacy sheet-data shape `RollBuilderFFG` /
    `rollSkillDirect` consume (same object the in-sheet roll handlers pass, e.g.
    `await this.getData({})` at actor-sheet-ffg.js:459). The fix is simply to
    `await` it: `const actorSheet = await ffgactor.sheet.getData();` in both
    generated command strings. (Foundry runs script-macro bodies as async, so
    top-level `await` is valid.)
- [ ] Re-run 0.2's dragged-macro execution for a skill dropped via **both** code
      paths (owned skill item drag *and* a `Transfer`-payload skill drag) to
      confirm each macro now rolls.

### 1.4 `ProseMirror` bare global — **verify-gated**

- [ ] **Verify:** In Stage 0.2, open the item editor and mount an inline
      `{{editor}}` field. If the console shows `ReferenceError: ProseMirror is
      not defined` (or a deprecation warning), proceed; if it mounts clean, move
      this whole item to Stage 2 as "cosmetic namespacing."
- [ ] If broken: replace the `ProseMirror.*` references with `foundry.prosemirror.*`
      (the namespaced form works on both V13 and V14) at:
      [item-editor.js:205,209](../../../modules/items/item-editor.js) and
      [ffg-document-sheet.js:575,579](../../../modules/apps/ffg-document-sheet.js)
      (`ProseMirrorMenu.build`, `defaultSchema`, `ProseMirrorKeyMaps.build`).
- [ ] Re-verify the editor mounts and saves.

### 1.5 `Hotbar` bare global in generated macro — **verify-gated**

- [ ] **Verify:** In 0.2, drag an *unowned* item to the hotbar and execute the
      macro (hits [macros.js:21](../../../modules/helpers/macros.js) —
      `await Hotbar.toggleDocumentSheet(...)`). If it throws under V14, replace
      `Hotbar` with the namespaced UI class (confirm the V14 path — likely
      `game.hotbar` instance or `foundry.applications.ui.Hotbar`; check what
      `toggleDocumentSheet` lives on in the running V14 build before editing).
- [ ] If it works, move to Stage 2.

### 1.6 `CanvasLayer` bare-global fallback — **module-load-time, verify-gated**

Unlike the other verify-gated items, this one runs at **import time**, not on a
user action, so a failure here aborts system init entirely (see Stage 0.1a).
Elevated from cosmetic cleanup to Stage 1 for that reason.

- [ ] **Verify (Stage 0.1a):** did load throw `ReferenceError: CanvasLayer is
      not defined` from [groupmanager-ffg.js:7](../../../modules/groupmanager-ffg.js)?
- [ ] Regardless of the result, drop the bare-global fallback — it is the only
      thing that can throw here. Resolve the class defensively without naming the
      bare global, e.g.
      `const CanvasLayerClass = foundry?.canvas?.layers?.CanvasLayer;`
      (and, if V14 moved it, point at the new location confirmed on the running
      build). Removing `|| CanvasLayer` is safe on V13 too.
- [ ] While here, confirm whether `GroupManagerLayer` is registered at all — the
      audit found **no** `CONFIG.Canvas.layers` registration; the Group Manager
      opens from the Destiny Tracker menu, not a canvas layer. If unregistered,
      delete `GroupManagerLayer` outright (this removes the risky line entirely).
      This supersedes Stage 2.5.

---

## Stage 2 — Nice-to-have (not required to function on V14)

Land as a separate PR after Stage 1 is verified green.

- [ ] **2.1** Delete the inert `CONFIG.ActiveEffect.legacyTransferral = false`
      at [swffg-main.js:113](../../../modules/swffg-main.js) — no-op under V14.
- [ ] **2.2** `foundry.applications.ux.TextEditor.enrichHTML(...)` →
      `.implementation.enrichHTML(...)` across ~52 sites (9 files). Already the
      namespaced form (not broken); `.implementation` honours `CONFIG.ux`
      overrides and matches the 2 sites that already do it
      ([popout-editor.js:165](../../../modules/popout-editor.js),
      [ffg-actor-sheet.js:168](../../../modules/apps/ffg-actor-sheet.js)).
      Pure consistency — do only if you want a warning-free future-proof.
- [ ] **2.3** `foundry.applications.apps.FilePicker` direct static/`new` →
      `.implementation` (8 sites) — same override-safety rationale.
- [ ] **2.4** Bare document instantiation → `.implementation`:
      `new Item(x)` builds the *base* class, bypassing `ItemFFG`; prefer
      `new Item.implementation(x)` / `Actor.implementation.create(...)`. ~24
      files. Behaves identically V13↔V14, so strictly optional; worth doing
      where the base-vs-subclass difference could bite
      (e.g. [item-sheet-ffg.js:1164,1195](../../../modules/items/item-sheet-ffg.js)
      `new Item(item).sheet.render(true)`).
- [ ] **2.5** *(Moved to Stage 1.6 — the `CanvasLayer` fallback runs at import
      time and can abort init, so it is no longer "nice-to-have".)*
- [ ] **2.6** Dedupe SlimSelect in [system.json:12,14](../../../system.json) —
      `lib/slimselect/slimselect.js` is listed in both `esmodules` and `scripts`.
- [ ] **2.7** Second-pass audit for V14 changes with no suspected usage but not
      yet grep-confirmed at runtime: Active Effect duration/expiration semantics,
      `RectangleShapeData` anchor change, `parseHTML` return-value change. None
      matched the codebase in the audit; re-check only if 0.2 shows odd behavior.

---

## Wrap-up

- [ ] Full smoke re-run on V14 with `compatibility.maximum: 14` — console clean.
- [ ] Push `V14-migration` (YeNov account) and open a fork-internal PR
      (`gh pr create --repo YeNov/StarWarsFFG`); switch back to `yehornovakov`.
- [ ] Optional: bump `version` in `system.json` if cutting a release.

## Audit evidence (2026-07-04)

- `342736e9` diff: 4 files, 8 insertions/8 deletions — matches the handoff's
  "8 sites / 4 files" claim, but the commit scope was **object helpers only**;
  it did not touch `randomID`.
- **Correction:** the initial "0 bare util-globals" reading was a *false
  negative* — it used a ripgrep negative-lookbehind pattern (`(?<![\w.])…`),
  which ripgrep's default engine does not support, so the search matched
  nothing. Re-audited with a `[^\w.]`-prefix pattern, which found **5 bare
  `randomID()`** calls: `import-helpers.js:1699,1712,1731,1743` and
  `actor-sheet-ffg.js:1598` (the `setProperty`/`duplicate` hits in
  `item-sheet-ffg.js:1244,1255` and `item-ffg.js:734` are comments). These
  throw `ReferenceError` on V14 → Stage 1.2.
- `.data.data` real-Document hits: `embeddeditem-helpers.js:227`,
  `macros.js:37-38` (generated macro string, `entity.type === "skill"` branch).
  The second skill branch (`macros.js:67-80`) already uses `.system` but shares
  the un-awaited-`getData()` defect. Other `.data.data` hits are plain
  sheet-context objects (`getData`/options bags), not Documents — safe.
- `getData()` is an intentional async bridge on the FFG V2 sheets
  (`ffg-document-sheet.js:163/188` → `_prepareContext`; `ActorSheetFFG.getData`
  is `async`), so the macro fix is `await`, not `_prepareContext()`.
- `ProseMirror` global: `item-editor.js`, `ffg-document-sheet.js`.
- `Hotbar` global: `macros.js:21` (generated macro).
- `CanvasLayer` bare-global fallback: `groupmanager-ffg.js:7` — **module-level**,
  eagerly imported by `swffg-main.js:28`, so it evaluates at load time.
- Dice terms: all 7 die types extend `foundry.dice.terms.DiceTerm`; `roll.js`
  uses namespaced `OperatorTerm`/`StringTerm`.
