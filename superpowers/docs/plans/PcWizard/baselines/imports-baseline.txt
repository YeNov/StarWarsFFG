# GATE-IMPORTS — pinned findings (plan DEV-17)
#
# Format, one pin per line:
#
#     <rule> | <file>:<line> | <issue> | <exact finding message>
#
# A pin is an EXACT rule:file:line triple. There is no rule-wide form and no file-wide form, and
# no wildcard is accepted — every rule keeps running over the whole tree. The gate fails if a
# pinned finding changes, moves, multiplies, or disappears, and it fails on any finding that is
# not pinned here.
#
# Only a finding confirmed REAL, PRE-EXISTING and OUT OF SCOPE may be pinned, and only with a
# tracking issue. A finding caused by a checker bug is FIXED, never pinned — that rule caught 30
# false positives on this checker's first genuine run.
#
# Recorded at Stage 1 (2026-07-20) on the untouched tree, without --cutover.
#
# ---------------------------------------------------------------------------------------------
# rule 2 · modules/importer/import-helpers.js:2961 · YeNov/StarWarsFFG#29
#   https://github.com/YeNov/StarWarsFFG/issues/29
#
#   `ImportHelpers.getTemplate()` fetches `systems/starwarsffg/template.json`, which commit
#   c8d29d86 deleted. That commit stated no runtime code read it; `swa-importer.js:573` does, via
#   `ImportHelpers.getTemplate("weapon")` — so this is a live defect in the Adversaries importer,
#   on the armed-adversary path only. Issue #29 also tracks a second live reference:
#   `.github/workflows/main.yml:123` still lists `template.json` in the release packaging list.
#
#   Out of scope for the PC Wizard rewrite: it is legacy-importer work, needs a DataModel-derived
#   replacement rather than a file read, and is unverifiable without a running world. Not fixed
#   here by owner decision.
# ---------------------------------------------------------------------------------------------

2 | modules/importer/import-helpers.js:2961 | YeNov/StarWarsFFG#29 | Foundry-style path "systems/starwarsffg/template.json" does not resolve under the repo root
